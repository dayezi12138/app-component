<resources>

    <!-- Base application theme. -->
    <style name="baseTheme" parent="@style/Base.Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@android:color/white</item>
        <item name="colorPrimaryDark">@android:color/white</item>
        <item name="colorAccent">@android:color/white</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="toolbarNavigationButtonStyle">@style/myToolbarNavigationButtonStyle</item>
    </style>
    <!--溢出[弹出]菜单样式 parent相当于继承【可以不要parent】-->
    <style name="OverflowMenuStyle" parent="@style/Widget.AppCompat.PopupMenu.Overflow">
        <!-- 是否覆盖锚点，默认为true，即盖住Toolbar -->
        <item name="overlapAnchor">false</item>
        <item name="android:dropDownWidth">wrap_content</item>
        <!--<item name="android:paddingRight">5dp</item>-->
        <!--<item name="android:layout_marginTop">5dp</item>-->
        <!-- 弹出层背景颜色 -->
        <!--<item name="android:popupBackground">@color/darkturquoise</item>-->
        <!-- 弹出层垂直方向上的偏移，即在竖直方向上距离Toolbar的距离，值为负则会盖住Toolbar -->
        <!--<item name="android:dropDownVerticalOffset">5dp</item>-->
        <!-- 弹出层水平方向上的偏移，即距离屏幕左边的距离，负值会导致右边出现空隙 -->
        <item name="android:dropDownHorizontalOffset">0dp</item>
        <!-- 设置弹出菜单文字颜色 -->
        <!--<item name="android:textColor">@color/white</item>-->
    </style>

    <style name="myToolbarNavigationButtonStyle" parent="@style/android:Widget.Material.Toolbar.Button.Navigation">
        <item name="android:minWidth">10dp</item>
        <item name="android:scaleType">centerInside</item>
        <item name="android:layout_marginLeft">@dimen/widget_margin_tiny</item>
    </style>

    <style name="order_line_layout_default_right_text_style">
        <item name="android:textSize">@dimen/font_min_xx_sp</item>
    </style>

    <style name="AppTheme.Toolbar" parent="Widget.AppCompat.ActionBar">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">?attr/actionBarSize</item>
        <item name="android:background">?attr/colorPrimary</item>
        <item name="android:theme">@style/ThemeOverlay.AppCompat.Dark.ActionBar</item>
    </style>
    <style name="AppTheme.Toolbar.Title">
        <item name="android:textSize">@dimen/font_large_sp</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>
