<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@mipmap/login_background"
    android:fitsSystemWindows="true"
    android:orientation="vertical">

    <ImageView
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="50dp"
        android:scaleType="fitXY"
        android:src="@mipmap/logo_1" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_margin="40dp"
        android:layout_weight="1"
        android:gravity="bottom|center"
        android:orientation="vertical">


        <Button
            android:id="@+id/normal_login_btn"
            style="@style/act_login_btn_style"
            android:background="@drawable/shape_act_login_btn_blue"
            android:text="@string/act_login_btn_normal_str"
            android:textColor="@color/white" />

        <Button
            android:id="@+id/phone_login_tbn"
            style="@style/act_login_btn_style"
            android:layout_marginTop="@dimen/widget_margin_tiny"
            android:background="@drawable/shape_act_login_btn_white"
            android:text="@string/act_login_btn_phone_str"
            android:textColor="@color/textColorPrimary" />
    </LinearLayout>
</LinearLayout>